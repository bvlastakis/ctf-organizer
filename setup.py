from distutils.core import setup
setup(name='build-applications',
      description='Builds GSA CTF applications for distribution.',
      author='Brian Vlastakis',
      packages=['build-applications']
)
